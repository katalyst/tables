<%# locals: { row:, resource: } %>

<% row.try(:selection) %>
<% row.cell :name, label: "Resource partial" do %>
  <%= resource.name %>
<% end %>
<% row.cell :active, class: "active" do |cell| %>
  <%= cell.value ? "Yes" : "No" %>
<% end %>
